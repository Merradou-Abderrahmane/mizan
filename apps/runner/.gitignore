/vendor/
/composer.lock
/.phpunit.result.cache
/.phpunit.cache/
/storage/runner-sqlite-*.sqlite

# Heavy fixtures — real Laravel apps, regenerated by bin/setup-heavy-fixtures.ps1
/tests/fixtures/valid_repo/
/tests/fixtures/stub_readme_repo/
/tests/fixtures/broken_deps_repo/
/tests/fixtures/broken_bootstrap_repo/
/tests/fixtures/broken_migration_repo/

# Committed light fixtures contain a working .git/ so the git checks can be
# exercised. Track their working-tree files but ignore the inner .git/ so
# the mizan repo doesn't trip its own embedded-repo detector.
/tests/fixtures/env_committed_repo/.git/
/tests/fixtures/single_commit_repo/.git/